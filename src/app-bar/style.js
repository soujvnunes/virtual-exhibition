import { makeStyles } from "@material-ui/core";

export default makeStyles(({ spacing }) => ({
  iconButtonAccessibility: {
    marginLeft: "auto",
  },
  iconButtonLogos: {
    padding: 0,
  },
  logos: {
    width: "auto",
    height: spacing(6),
  },
}));
